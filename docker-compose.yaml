version: "3.9"

services:
  web:
    build: .
    container_name: meta_vibes
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app   
    restart: unless-stopped
